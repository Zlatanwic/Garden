---
title: CS61c Floating Point
date: 2025-11-26
---

# Floating Point
## 基础定点表示法
95% of the folks out
there are completely
clueless about floatingpoint." author="James Gosling, 1998-02-28"

### 我们可以用N bits来表示什么呢？
+ Unsigned integers: 0 to $2^N -1$
+ Signed integers(2进制补码): $-2^{N-1}$ to $2^{N-1} -1$

### 但是其他的数字呢？
1. 比如说很大的数字 31,556,926,00010 ($3.155692610 \times 10^{10}$)
2. 或者很小的数字 0.000000000052917710m ($5.2917710 \times 10^{-11}$)
3. 还有 **小数以及分数** 呢？

我们接下来将介绍分数（小数）的表示方法，这也会对第1，2点的解决有所帮助

### 分数的表示
"2进制小数点"像十进制一样都是数字整数部分和小数部分的分割线
以6-bit表示为例
<span class="bc">xx.yyyy</span>

$10.1010_2 = 1 \times 2^1 + 1 \times 2^{-1} +1 \times 2^{-3} =2.625_{10}$
如果我们假设这是一个“定点二进制小数点”，那么这6比特位可以代表从0到3.9375（接近4）这样的范围
:::info
显然我们觉得这并不是一个很好的实践
:::
## 浮点表示法
### 科学计数法
目前为止，在我们的例子中，我们用的都是定点表示，但是我们真正想要的是将小数点 **float** （浮动）起来。但是为什么呢？
+ 浮点数可以极大地提升储存数据的效率（储存相同数据占用的比特位更少），因此也就增加了数据表示的的精准度
  
:::details
比如，我们将一个十进制0.1640625储存进计算机（2进制表示）
标准(normal)格式：$+1.xxxx...x_{two} * 2^{yyy...yy_{two}}$
![fp32](/fp32.png)
32位浮点数最小（绝对值）可以到$1.2 \times 10^{-38}$，最大可以到$3.4 \times 10^{38}$
:::tip
注意，在normal格式下，有效位小数点前默认的`"1"`是省略不存储的
:::
+ 如果结果太大呢？（绝对值超出了可表示范围$3.4 \times 10^{38}$ ）

则会导致`overflow`！
:::warning
Overflow!->指数大于8比特位可表示的范围
:::
+ 如果结果太小呢？绝对值小于可表示范围$1.2 \times 10^{-38}$ ）

则会导致`underflow `!
:::warning
Underflow!->负向指数超过8比特位可表示的范围
:::
![outflow](/outflow.png)
一定要搞清楚`overflow`和`underflow`的差别，负向的超限也叫overflow而只有趋向于0才叫underflow 

所以,总结下来，1位的符号位，1表示负数，0表示正数；

对于有效位(Significand)：
+ 对于normalized numers，1会默认不储存
+ 换成double可以有52位储存，会更精确
+ 通常有效位会大于0小于1（normalized）
:::info
但是，产生了一个问题：对于数字0，他并没有前导的1，所以我们约定将所有指数位(exponent)置为0表示0，但是问题又出现了，不同的sign位还会导致“两个零”问题的出现
:::
于是，我们引入了另一个IEEE754中很重要的概念`偏移指数（biased exponent）`

设计者们希望浮点数可以在没有专门的浮点数硬件的条件下仍然可以使用，想用大的指数代表大的数字，指数越大数值则越大，我们需要一个像`里程表式`的指数位，从最小达到最大可以表示整体数值的最小到最大，显然2进制补码等等都完成不了这个任务，我们在这里使用`移码`的概念，从000...000到1111...111就是表示一个线性增加的过程，那么我们就需要一个偏移量来让它可以表示负值。在ieee754中，我们规定了一个负向的偏移量-127。

至于为什么是负向127，首先是ieee754规定的对于n位，偏移量为$2^{n-1}-1$，其次，感性地来理解，我们希望指数的正负部分是对称的，在增长到10000000的时候实际值恰好从负数变成0

所以，最终我们的公式(32-bit)就是
$$(-1)^S \times (1+Significand) \times 2^{(Exponent-127)}$$

## 特殊数字
### “无穷”的表示方法
首先，表示“无穷”是很有必要的，比如我们在程序的某个部分要维护一个max
```c++{1}
int my_max=MIN;  //MIN代表无穷小
for(int i=0;i<n;i++){
    //a是一个数组，n是元素个数
    my_max=max(my_max,a[i]);
}
```
如果我们可以用`MIN`来代表无穷小，那么`a[0]`首先一定会被保存到`my_max`因为无穷小肯定比`a[0]`要小，这样就可以很自然的向后遍历。
同时，X/0>Y可能也是个合法的比较。所以在浮点数里表示无穷还是很有必要的。

### IEEE754表示无穷
+ 如果无穷是合法的，这个结果也理应是合法的，我们称其为<span class="highlight">Not a Number</span>
+ 表示为指数全1，有效位不全0

#### 为什么是有意义的？
鉴于有效位有23位，我们可以有$2^{23}-1$种NaN，如果这都是没意义的，那么闲的我们对空间的利用效率太低了！
+ 我们希望NaN可以对debug有所帮助
+ 比如，NaN是具有传播性的，operate(NaN,x)=NaN，NaN和任何数做运算结果都是NaN，所以我们如果得到一个NaN的结果说明整个计算过程中必然有一步产生了不合理的NaN结果（但不会error）
+ 我们还可以用NaN的非0有效位去表示产生了那种错误，比如对0/0进行一种encode放到有效位里，把“对读书开根号”进行一种encode放入有效位里，这样我们后续通过decode有效位的错误信息编码就可以知晓前面运算发生了哪种错误

### 非规格化数字的表示(Denorms)
:::danger 我们还有一个问题
在0和规格浮点数(normalized)之间有一个很大的缝隙gap！
:::
+ 可表示的最小规格数为：a=$1.000.._2 * 2^{-126}$=$2^{-126}$
+ 可表示的第二小的规格数为：n=$1.000...01_2 * 2^{-126}=(1+0.00...1_2) * 2^{-126}=(1+2^{-23}) * 2^{-126}=2^{-126}+2^{-149}$
+ a-0=$2^{-126}$
+ b-a=$2^{-149}$
+ 0和可表示最小规格数的距离是下一个距离的$2^{23}$倍！这真的是一个很大的gap!

<span class="highlight">造成这样的罪魁祸首其实是隐式省略的1！</span>

那我们如何解决这个问题呢？
:::tip 
我们发现我们还没有用指数全为1，有效位不全0的情况，我们可以用在这里
:::
所以denormalized number就是取消了前导1，在数值上默认指数值为-126（但其实实际储存是全0）
+ 这样的，最小的非规格数就是$2^{-149} \times 2^{-23}$
+ 第二小的非规格数就是$2^{-148} \times 2^{-23}$
+ 在一段范围内“步幅”都是$2^{-149} \times 2^{-23}$,当有效位增长到111...11时，再“迈一步”就会进一位到指数位，指数位+1，步幅变成$2^{-125} \times 2^{-23}$了，然后有效位继续从0增长到全一，然后步幅再一次翻倍。总体来说，在一个阶段内，共走了$2^{23}-1$步，步幅从最开始$2^{-149} \times 2^{-23}$，每走$2^{23}-1$步就乘2，那么可以预想到终有一步，步幅会大于1，这就以为这从这里开始就有整数不能用float表示
+ 那么什么时候步幅为1呢？显然是指数值是$2^{23}$的时候这样和有效位的$2^{-23}$乘起来才会是1
![stride=1](/stride1.png)
此时有效位全1，再迈一步，进位指数+1，有效位全0，步幅变为2，结果变为16777216，再迈一步此时步幅是2，结果变为16777218，如图：
![stride=2](/stride2.png)
我们“神奇”地发现16777217“消失”了，其实是float到这里精度已经很小了，储存不了166777217了，换成double的话保持精确的范围还会更大一点

最后总结一下：
![consulution](/conl.png)

[ieee754模拟器](https://www.h-schmidt.net/FloatConverter/IEEE754.html)




<style>
    .bc{
    color:skyblue;
    display: block;     /* 让 span 变成块级元素 */
    text-align: center; /* 居中文字 */
    }

    .highlight{
        color:orange;
    }

    </style>
